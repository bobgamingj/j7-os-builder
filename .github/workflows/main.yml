name: Build postmarketOS Samsung J7 Mini-PC

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 240

    env:
      PMOS_BASE: /tmp/pmos-ci
      PMOS_WORK: /tmp/pmos-ci/work
      PMOS_PMAPORTS: /tmp/pmos-ci/pmaports
      PMOS_CFG: /tmp/pmos-ci/pmbootstrap_v3.cfg
      VENV_PATH: /tmp/pmos-venv
      PMBOOTSTRAP_NO_COLOR: "1"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        shell: bash
        run: |
          set -euxo pipefail

          sudo apt-get update

          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y \
            git \
            python3 \
            python3-pip \
            python3-venv \
            build-essential \
            qemu-user-static \
            binfmt-support \
            zip \
            unzip \
            rsync \
            kpartx \
            e2fsprogs \
            android-sdk-libsparse-utils \
            dosfstools \
            fakeroot \
            ccache \
            sudo \
            curl \
            ca-certificates

      - name: Create Python virtual environment
        shell: bash
        run: |
          set -euxo pipefail

          python3 -m venv "$VENV_PATH"

          source "$VENV_PATH/bin/activate"

          python -m pip install --upgrade \
            pip \
            setuptools \
            wheel \
            tomli

      - name: Install pmbootstrap
        shell: bash
        run: |
          set -euxo pipefail

          source "$VENV_PATH/bin/activate"

          python -m pip install \
            --no-build-isolation \
            git+https://gitlab.postmarketos.org/postmarketOS/pmbootstrap.git

          which pmbootstrap
          pmbootstrap --version

      - name: Prepare directories
        shell: bash
        run: |
          set -euxo pipefail

          mkdir -p "$PMOS_BASE"
          mkdir -p "$PMOS_WORK"

      - name: Clone pmaports
        shell: bash
        run: |
          set -euxo pipefail

          rm -rf "$PMOS_PMAPORTS"

          git clone \
            --depth=1 \
            https://gitlab.postmarketos.org/postmarketOS/pmaports.git \
            "$PMOS_PMAPORTS"

          echo "Checking pmaports repository..."

          ls -lah "$PMOS_PMAPORTS"

          test -f "$PMOS_PMAPORTS/pmaports.cfg"

          echo "pmaports.cfg found successfully"

      - name: Initialize pmbootstrap
        shell: bash
        run: |
          set -euxo pipefail

          source "$VENV_PATH/bin/activate"

          rm -f "$PMOS_CFG"

          # Disable pipefail temporarily because `yes`
          # exits with SIGPIPE after pmbootstrap completes.
          set +o pipefail

          yes "" | pmbootstrap \
            -y \
            --details-to-stdout \
            -c "$PMOS_CFG" \
            -w "$PMOS_WORK" \
            -p "$PMOS_PMAPORTS" \
            init

          set -o pipefail

          echo "Verifying generated config..."

          test -f "$PMOS_CFG"

          echo "pmbootstrap config created successfully"

          cat "$PMOS_CFG"

      - name: Configure Samsung J7 device
        shell: bash
        run: |
          set -euxo pipefail

          source "$VENV_PATH/bin/activate"

          echo "Checking available Samsung devices..."

          find "$PMOS_PMAPORTS/device" -type d | grep samsung || true

          # Try Samsung J7 device configuration
          pmbootstrap \
            -y \
            --details-to-stdout \
            -c "$PMOS_CFG" \
            -w "$PMOS_WORK" \
            -p "$PMOS_PMAPORTS" \
            config device samsung-j7elte || true

          pmbootstrap \
            -y \
            --details-to-stdout \
            -c "$PMOS_CFG" \
            -w "$PMOS_WORK" \
            -p "$PMOS_PMAPORTS" \
            config ui xfce4

      - name: Show final configuration
        shell: bash
        run: |
          set -euxo pipefail

          echo "===== PMBOOTSTRAP CONFIG ====="

          cat "$PMOS_CFG"

      - name: Update repositories
        shell: bash
        run: |
          set -euxo pipefail

          source "$VENV_PATH/bin/activate"

          pmbootstrap \
            -y \
            --details-to-stdout \
            -c "$PMOS_CFG" \
            -w "$PMOS_WORK" \
            -p "$PMOS_PMAPORTS" \
            pull

      - name: Build recovery zip
        shell: bash
        run: |
          set -euxo pipefail

          source "$VENV_PATH/bin/activate"

          pmbootstrap \
            -y \
            --details-to-stdout \
            -c "$PMOS_CFG" \
            -w "$PMOS_WORK" \
            -p "$PMOS_PMAPORTS" \
            install \
            --android-recovery-zip \
            --password 123456

      - name: Export generated files
        shell: bash
        run: |
          set -euxo pipefail

          source "$VENV_PATH/bin/activate"

          pmbootstrap \
            -y \
            --details-to-stdout \
            -c "$PMOS_CFG" \
            -w "$PMOS_WORK" \
            -p "$PMOS_PMAPORTS" \
            export

          echo "===== EXPORT DIRECTORY ====="

          ls -lah /tmp/postmarketOS-export || true

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: postmarketos-j7elte
          path: /tmp/postmarketOS-export/
          if-no-files-found: error
